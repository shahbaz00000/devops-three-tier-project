name: CI workflow

on:
  push:

jobs:
  build:
      runs-on: ubuntu-latest # Github runner
      steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # clone the code into a ububtu server

      - name: Setup Docker
        uses: docker/setup-buildx-action@v4